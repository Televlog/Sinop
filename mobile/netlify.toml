[build]
  command = "npm install && npx expo install react-native-web@~0.19.10 react-dom@18.2.0 @expo/metro-runtime && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
